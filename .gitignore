# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# Virtual environments
.venv/
venv/
env/

# uv
# uv.lock IS committed — it pins the exact dependency set for reproducible installs.

# Testing / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/
.coverage
htmlcov/
coverage.xml

# Environment / secrets
# No secrets are needed by this project, but a stray .env must never be committed.
.env
.env.*
!.env.example

# Chrome scraping profile (created at runtime, contains the operator's session)
Chrome-Scraping/
chrome-scraping/

# Editors
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Local scratch
/tmp/
scratch/
*.log

# Operational note for the release run, not part of the project.
PUBLISH_PROMPT.md

# Agent scaffolding from the autonomous improvement run — not part of the project.
.agent/
.qwen/
MISSION.md
QWEN.md
start-run.sh

# Архивы релиза собираются и прикладываются к Releases, а не хранятся в git.
*.zip

# jsdom для DOM-тестов ставится из package.json + package-lock.json: оба
# отслеживаются намеренно (CI кладёт jsdom по этим манифестам, иначе
# dom-тесты экстракторов тихо скипаются на раннерах).
node_modules/

# dsh/package.json is not the Node artefact above: it is the plugin-bundle
# manifest every dsh install resolves, so it must be tracked.
!dsh/package.json
