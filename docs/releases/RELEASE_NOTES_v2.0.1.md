# v2.0.1

Patch release for v2 registry delivery:

- corrected the Docker stdio probe to expect the v2 unified surface of 37 tools;
- no product behavior changed from v2.0.0.

Verified: full offline suite, cross-platform CI, Docker stdio probe, and MCP Registry publish.
